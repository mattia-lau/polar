{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build --stream",
    "start:dev": "lerna run start:dev --stream"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
